import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-final-cut-teaser',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './final-cut-teaser.html',
  styleUrl: './final-cut-teaser.scss',
})
export class FinalCutTeaserComponent {}
